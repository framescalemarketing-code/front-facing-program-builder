--- a/src/style.css
+++ b/src/style.css
@@ -3,6 +3,7 @@
 @custom-variant dark (&:is(.dark *));
 
 :root {
+  color-scheme: light;
   --font-sans: "Univers LT Std", "Segoe UI", system-ui, -apple-system, sans-serif;
   --font-size: 16px;
 
@@ -8,8 +9,8 @@
   /* Brand palette */
   --brand-navy: #244093;
   --brand-blue: #2971b5;
-  --brand-gold: #fbb635;
   --brand-link: #0071ce;
   --brand-sky: #5e97dd;
   --brand-cream: #ffedce;
@@ -18,6 +19,7 @@
   --brand-charcoal: #333333;
   --brand-gray: #f2f2f2;
   --brand-white: #ffffff;
+  --brand-gold: #fbb635;
 
   /* Core surfaces */
   --background: var(--brand-white);
@@ -35,7 +37,7 @@
 
   /* Muted */
   --muted: var(--brand-gray);
-  --muted-foreground: #475569;
+  --muted-foreground: var(--brand-charcoal);
 
   /* Accent */
   --accent: var(--brand-cream);
@@ -55,10 +57,10 @@
 
   /* Borders and inputs */
-  --border: rgba(0, 9, 47, 0.14);
+  --border: rgba(0, 9, 47, 0.18);
   --input: transparent;
-  --input-background: #f6f7f9;
-  --switch-background: #c9d3e3;
+  --input-background: #ffffff;
+  --switch-background: var(--brand-sky);
 
   /* Typography */
   --font-weight-medium: 500;
@@ -73,19 +75,19 @@
   --chart-1: var(--brand-navy);
   --chart-2: var(--brand-blue);
   --chart-3: var(--brand-link);
   --chart-4: var(--brand-sky);
-  --chart-5: var(--brand-gold);
+  --chart-5: var(--brand-cream);
 
   /* Radius */
-  --radius: 0.625rem;
+  --radius: 0.5rem;
 
   /* Sidebar */
   --sidebar: var(--brand-white);
   --sidebar-foreground: var(--brand-ink);
   --sidebar-primary: var(--brand-navy);
   --sidebar-primary-foreground: var(--brand-white);
@@ -124,15 +126,15 @@
   --destructive: #d4183d;
   --destructive-foreground: var(--brand-white);
 
-  --border: rgba(255, 255, 255, 0.14);
+  --border: rgba(255, 255, 255, 0.2);
   --input: rgba(255, 255, 255, 0.10);
   --input-background: rgba(255, 255, 255, 0.08);
   --switch-background: rgba(94, 151, 221, 0.50);
 
   --ring: var(--brand-link);
 
   --chart-1: var(--brand-sky);
   --chart-2: var(--brand-blue);
   --chart-3: var(--brand-link);
-  --chart-4: var(--brand-gold);
+  --chart-4: var(--brand-sky);
   --chart-5: var(--brand-cream);
 
   --sidebar: rgba(255, 255, 255, 0.06);
@@ -174,13 +176,139 @@
   --color-sidebar-border: var(--sidebar-border);
   --color-sidebar-ring: var(--sidebar-ring);
 }
 
+html,
+body,
+#root {
+  width: 100%;
+  min-height: 100%;
+}
+
 body {
+  margin: 0;
   font-family: var(--font-sans);
+  font-size: var(--font-size);
   background: var(--color-background);
   color: var(--color-foreground);
-  line-height: 1.5;
+  line-height: 1.45;
   text-rendering: optimizeLegibility;
   -webkit-font-smoothing: antialiased;
+  overflow-x: clip;
+}
+
+#root {
+  min-height: 100dvh;
+  overflow-x: clip;
+}
+
+main {
+  background: #f8f9fc;
+  overflow-x: clip;
+}
+
+img,
+svg,
+video,
+canvas {
+  max-width: 100%;
+}
+
+input[type="checkbox"] {
+  accent-color: var(--brand-sky);
+}
+
+:is(.ui-card, .ui-card-shell, .bg-card)
+  :is(input:not([type="checkbox"]):not([type="radio"]), select, textarea):not(
+    :is(.ui-card-inset *, .ui-card-soft *, .bg-secondary *, .bg-muted *)
+  ) {
+  border-color: color-mix(in srgb, var(--brand-blue) 30%, var(--color-border));
+  background: #f6f8fc;
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.85), 0 1px 2px rgba(0, 9, 47, 0.05);
+}
+
+:is(.ui-card, .ui-card-shell, .bg-card)
+  :is(input:not([type="checkbox"]):not([type="radio"]), select, textarea):not(
+    :is(.ui-card-inset *, .ui-card-soft *, .bg-secondary *, .bg-muted *)
+  ):hover {
+  border-color: color-mix(in srgb, var(--brand-blue) 48%, var(--color-border));
+}
+
+:is(.ui-card, .ui-card-shell, .bg-card)
+  :is(input:not([type="checkbox"]):not([type="radio"]), select, textarea):not(
+    :is(.ui-card-inset *, .ui-card-soft *, .bg-secondary *, .bg-muted *)
+  ):disabled {
+  background: #edf1f7;
+}
+
+input::placeholder,
+textarea::placeholder {
+  color: color-mix(in srgb, var(--color-muted-foreground) 72%, transparent);
 }
 
 @media (max-width: 640px) {
   input,
   select,
@@ -205,3 +333,97 @@
   left: 0.75rem;
   box-shadow: 0 0 0 3px var(--color-ring);
   outline: none;
 }
+
+h1,
+h2,
+h3 {
+  font-family: var(--font-sans);
+  font-weight: 700;
+  letter-spacing: -0.015em;
+}
+
+.brand-eyebrow {
+  font-size: 0.72rem;
+  line-height: 1;
+  letter-spacing: 0.12em;
+  text-transform: uppercase;
+  font-weight: 700;
+  color: color-mix(in srgb, var(--brand-navy) 88%, black);
+}
+
+.brand-heading {
+  text-transform: uppercase;
+  letter-spacing: 0.01em;
+  line-height: 1.05;
+}
+
+.brand-grid-bg {
+  background-image: repeating-linear-gradient(
+    to right,
+    transparent 0,
+    transparent 55px,
+    color-mix(in srgb, var(--brand-blue) 8%, transparent) 55px,
+    color-mix(in srgb, var(--brand-blue) 8%, transparent) 56px
+  );
+}
+
+.ui-card-shell {
+  border: 1px solid var(--color-border);
+  border-radius: 0.75rem;
+  background: var(--color-card);
+  box-shadow: 0 4px 14px rgba(36, 64, 147, 0.06);
+}
+
+.ui-card {
+  border: 1px solid color-mix(in srgb, var(--color-border) 95%, transparent);
+  border-radius: 0.75rem;
+  background: var(--color-card);
+  box-shadow: 0 1px 4px rgba(36, 64, 147, 0.05);
+}
+
+.ui-card-soft {
+  border: 1px solid color-mix(in srgb, var(--color-border) 90%, transparent);
+  border-radius: 0.625rem;
+  background: color-mix(in srgb, var(--color-secondary) 55%, var(--color-card));
+}
+
+.ui-card-inset {
+  border: 1px solid color-mix(in srgb, var(--color-border) 95%, transparent);
+  border-radius: 0.625rem;
+  background: color-mix(in srgb, var(--color-background) 78%, var(--color-secondary));
+  padding: 0.625rem 0.75rem;
+}
+
+.ui-card-compact {
+  padding: 0.625rem 0.75rem;
+}
+
+.ui-card-regular {
+  padding: 0.75rem 0.875rem;
+}
+
+.ui-card-interactive {
+  transition: border-color 150ms ease, background-color 150ms ease, box-shadow 150ms ease;
+}
+
+.ui-card-interactive:hover {
+  border-color: color-mix(in srgb, var(--color-primary) 32%, var(--color-border));
+  background: color-mix(in srgb, var(--color-primary) 4%, var(--color-card));
+  box-shadow: 0 2px 8px rgba(36, 64, 147, 0.07);
+}
+
+@media (min-width: 640px) {
+  .ui-card-compact {
+    padding: 0.75rem 0.875rem;
+  }
+
+  .ui-card-regular {
+    padding: 0.875rem 1rem;
+  }
+}
--- a/src/app/AppShell.tsx
+++ b/src/app/AppShell.tsx
@@ -44,7 +44,7 @@
 
   return (
-    <div className="min-h-screen bg-background text-foreground">
+    <div className="min-h-dvh overflow-x-clip bg-background text-foreground">
       <a href="#main-content" className="skip-link">
         Skip to main content
       </a>
@@ -52,10 +52,10 @@
 
       <header
-        className="border-b border-border bg-card text-card-foreground app-shell-header no-print"
+        className="app-shell-header no-print border-b border-border bg-[color:var(--brand-blue)] text-white shadow-sm"
         data-pdf-exclude="true"
       >
-        <div className="mx-auto flex max-w-7xl items-center justify-between gap-3 px-4 py-3 sm:px-6 lg:px-8">
-          <div className="flex items-center gap-3">
+        <div className="mx-auto flex w-full max-w-7xl flex-wrap items-center justify-between gap-3 px-4 py-2.5 sm:px-6 lg:px-8">
+          <div className="flex min-w-0 items-center gap-3">
             <button
               type="button"
               onClick={() => onNavigate("builder", "internal")}
@@ -66,19 +66,23 @@
               <img
                 src="/brand/osso/osso-logo-horizontal.png"
                 alt="On-Sight Safety Optics"
-                className="hidden h-10 w-auto sm:block"
+                className="hidden h-7 w-auto sm:block sm:h-8"
                 loading="eager"
               />
               <img
                 src="/brand/osso/osso-logomark.svg"
                 alt="OSSO logo mark"
-                className="block h-10 w-auto sm:hidden"
+                className="block h-7 w-auto sm:hidden"
                 loading="eager"
               />
               <span className="sr-only">On-Sight Safety Optics</span>
             </button>
-            <div className="text-sm font-semibold">On-Sight Safety Optics</div>
+            <div className="text-xs font-semibold tracking-[0.08em] uppercase sm:text-sm">
+              On-Sight Safety Optics
+            </div>
           </div>
-          <div className="hidden text-xs text-muted-foreground sm:block">Program Planning Tool</div>
+          <div className="hidden text-xs text-white/90 sm:block">Program Planning Tool</div>
         </div>
       </header>
 
-      <main id="main-content" className="bg-background">
+      <main id="main-content" className="overflow-x-clip bg-background">
         <Suspense
           fallback={
-            <div className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8">
-              <div className="rounded-lg border border-border bg-card p-6 text-sm text-muted-foreground">Loading page...</div>
+            <div className="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
+              <div className="rounded-lg border border-border bg-card p-4 text-sm text-muted-foreground">
+                Loading page...
+              </div>
             </div>
           }
         >